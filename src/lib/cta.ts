/**
 * Todos os CTAs do site levam ao formulário de inscrição, que fica no topo
 * (bloco com `id="formulario"`, dentro da Hero). O scroll suave já vem do
 * `html { scroll-behavior: smooth }` em index.css.
 */
export const FORM_HREF = "#formulario";
